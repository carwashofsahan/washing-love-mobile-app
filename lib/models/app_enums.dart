// all enums
enum UserState { PENDING, ACCEPTED, REJECTED, DELETED, LOCK, UNLOCK }

enum WashCenterState { ACTIVE, PENDING, DELETED }

enum PackageStatus { PENDING, ACCEPTED, REJECTED, DELETED }

enum NotificationState { SEEN, UNSEEN }

enum BookingStatus { COMPLETED, PENDING, PROCESSING, REJECTED }
